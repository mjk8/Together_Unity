
/// <summary>
/// 내 캐릭, 다른 캐릭이 공통으로 사용하는 정보를 담는 클래스 (주로 판정, 애니메이션 관련)
/// </summary>
public class PlayerStatus
{
    public bool _isCurrentTrapped = false; //현재 트랩에 걸렸는지 여부

    public bool _isFlashed = false; //현재 플래시에 맞았는지 여부
}